# Upload a pod with existing tag using --force
# [--fleet-root, <FLEET_ROOT>, pod, upload, <FLEET_ROOT>/test-pod.json, --tag, some-tag, --force]
Successfully uploaded test-pod#3 to fleet -={|~~~~~~~~~~FLEET_ID~~~~~~~~~~|}=-.
  id: -={|~~~~~~~ID test-pod#3~~~~~~~~|}=-
  references:
    - test-pod@latest
    - test-pod@some-tag
